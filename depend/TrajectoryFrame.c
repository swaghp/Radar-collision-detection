// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by lcm-gen

#include <string.h>
#include "TrajectoryFrame.h"

static int __TrajectoryFrame_hash_computed;
static uint64_t __TrajectoryFrame_hash;

uint64_t __TrajectoryFrame_hash_recursive(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __TrajectoryFrame_get_hash)
            return 0;

    __lcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__TrajectoryFrame_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0x7d050b5fe70c5930LL
         + __Point_hash_recursive(&cp)
         + __int16_t_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __TrajectoryFrame_get_hash(void)
{
    if (!__TrajectoryFrame_hash_computed) {
        __TrajectoryFrame_hash = (int64_t)__TrajectoryFrame_hash_recursive(NULL);
        __TrajectoryFrame_hash_computed = 1;
    }

    return __TrajectoryFrame_hash;
}

int __TrajectoryFrame_encode_array(void *buf, int offset, int maxlen, const TrajectoryFrame *p, int elements)
{
    int pos = 0, element;
    int thislen;

    for (element = 0; element < elements; element++) {

        thislen = __Point_encode_array(buf, offset + pos, maxlen - pos, &(p[element].point), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].heading), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int TrajectoryFrame_encode(void *buf, int offset, int maxlen, const TrajectoryFrame *p)
{
    int pos = 0, thislen;
    int64_t hash = __TrajectoryFrame_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __TrajectoryFrame_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int __TrajectoryFrame_encoded_array_size(const TrajectoryFrame *p, int elements)
{
    int size = 0, element;
    for (element = 0; element < elements; element++) {

        size += __Point_encoded_array_size(&(p[element].point), 1);

        size += __int16_t_encoded_array_size(&(p[element].heading), 1);

    }
    return size;
}

int TrajectoryFrame_encoded_size(const TrajectoryFrame *p)
{
    return 8 + __TrajectoryFrame_encoded_array_size(p, 1);
}

int __TrajectoryFrame_decode_array(const void *buf, int offset, int maxlen, TrajectoryFrame *p, int elements)
{
    int pos = 0, thislen, element;

    for (element = 0; element < elements; element++) {

        thislen = __Point_decode_array(buf, offset + pos, maxlen - pos, &(p[element].point), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].heading), 1);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __TrajectoryFrame_decode_array_cleanup(TrajectoryFrame *p, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        __Point_decode_array_cleanup(&(p[element].point), 1);

        __int16_t_decode_array_cleanup(&(p[element].heading), 1);

    }
    return 0;
}

int TrajectoryFrame_decode(const void *buf, int offset, int maxlen, TrajectoryFrame *p)
{
    int pos = 0, thislen;
    int64_t hash = __TrajectoryFrame_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __TrajectoryFrame_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int TrajectoryFrame_decode_cleanup(TrajectoryFrame *p)
{
    return __TrajectoryFrame_decode_array_cleanup(p, 1);
}

int __TrajectoryFrame_clone_array(const TrajectoryFrame *p, TrajectoryFrame *q, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        __Point_clone_array(&(p[element].point), &(q[element].point), 1);

        __int16_t_clone_array(&(p[element].heading), &(q[element].heading), 1);

    }
    return 0;
}

TrajectoryFrame *TrajectoryFrame_copy(const TrajectoryFrame *p)
{
    TrajectoryFrame *q = (TrajectoryFrame*) malloc(sizeof(TrajectoryFrame));
    __TrajectoryFrame_clone_array(p, q, 1);
    return q;
}

void TrajectoryFrame_destroy(TrajectoryFrame *p)
{
    __TrajectoryFrame_decode_array_cleanup(p, 1);
    free(p);
}

int TrajectoryFrame_publish(lcm_t *lc, const char *channel, const TrajectoryFrame *p)
{
      int max_data_size = TrajectoryFrame_encoded_size (p);
      uint8_t *buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = TrajectoryFrame_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = lcm_publish (lc, channel, buf, data_size);
      free (buf);
      return status;
}

struct _TrajectoryFrame_subscription_t {
    TrajectoryFrame_handler_t user_handler;
    void *userdata;
    lcm_subscription_t *lc_h;
};
static
void TrajectoryFrame_handler_stub (const lcm_recv_buf_t *rbuf,
                            const char *channel, void *userdata)
{
    int status;
    TrajectoryFrame p;
    memset(&p, 0, sizeof(TrajectoryFrame));
    status = TrajectoryFrame_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding TrajectoryFrame!!!\n", status);
        return;
    }

    TrajectoryFrame_subscription_t *h = (TrajectoryFrame_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    TrajectoryFrame_decode_cleanup (&p);
}

TrajectoryFrame_subscription_t* TrajectoryFrame_subscribe (lcm_t *lcm,
                    const char *channel,
                    TrajectoryFrame_handler_t f, void *userdata)
{
    TrajectoryFrame_subscription_t *n = (TrajectoryFrame_subscription_t*)
                       malloc(sizeof(TrajectoryFrame_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->lc_h = lcm_subscribe (lcm, channel,
                                 TrajectoryFrame_handler_stub, n);
    if (n->lc_h == NULL) {
        fprintf (stderr,"couldn't reg TrajectoryFrame LCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int TrajectoryFrame_subscription_set_queue_capacity (TrajectoryFrame_subscription_t* subs,
                              int num_messages)
{
    return lcm_subscription_set_queue_capacity (subs->lc_h, num_messages);
}

int TrajectoryFrame_unsubscribe(lcm_t *lcm, TrajectoryFrame_subscription_t* hid)
{
    int status = lcm_unsubscribe (lcm, hid->lc_h);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe TrajectoryFrame_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

