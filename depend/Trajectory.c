// THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
// BY HAND!!
//
// Generated by lcm-gen

#include <string.h>
#include "Trajectory.h"

static int __Trajectory_hash_computed;
static uint64_t __Trajectory_hash;

uint64_t __Trajectory_hash_recursive(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __Trajectory_get_hash)
            return 0;

    __lcm_hash_ptr cp;
    cp.parent =  p;
    cp.v = (void*)__Trajectory_get_hash;
    (void) cp;

    uint64_t hash = (uint64_t)0xb72c790e09cba21cLL
         + __int16_t_hash_recursive(&cp)
         + __int16_t_hash_recursive(&cp)
         + __int16_t_hash_recursive(&cp)
         + __int16_t_hash_recursive(&cp)
         + __TrajectoryFrame_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __Trajectory_get_hash(void)
{
    if (!__Trajectory_hash_computed) {
        __Trajectory_hash = (int64_t)__Trajectory_hash_recursive(NULL);
        __Trajectory_hash_computed = 1;
    }

    return __Trajectory_hash;
}

int __Trajectory_encode_array(void *buf, int offset, int maxlen, const Trajectory *p, int elements)
{
    int pos = 0, element;
    int thislen;

    for (element = 0; element < elements; element++) {

        thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].speed), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].distance), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].mode), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].count), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __TrajectoryFrame_encode_array(buf, offset + pos, maxlen - pos, p[element].trajectory, p[element].count);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int Trajectory_encode(void *buf, int offset, int maxlen, const Trajectory *p)
{
    int pos = 0, thislen;
    int64_t hash = __Trajectory_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __Trajectory_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int __Trajectory_encoded_array_size(const Trajectory *p, int elements)
{
    int size = 0, element;
    for (element = 0; element < elements; element++) {

        size += __int16_t_encoded_array_size(&(p[element].speed), 1);

        size += __int16_t_encoded_array_size(&(p[element].distance), 1);

        size += __int16_t_encoded_array_size(&(p[element].mode), 1);

        size += __int16_t_encoded_array_size(&(p[element].count), 1);

        size += __TrajectoryFrame_encoded_array_size(p[element].trajectory, p[element].count);

    }
    return size;
}

int Trajectory_encoded_size(const Trajectory *p)
{
    return 8 + __Trajectory_encoded_array_size(p, 1);
}

int __Trajectory_decode_array(const void *buf, int offset, int maxlen, Trajectory *p, int elements)
{
    int pos = 0, thislen, element;

    for (element = 0; element < elements; element++) {

        thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].speed), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].distance), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].mode), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].count), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        p[element].trajectory = (TrajectoryFrame*) lcm_malloc(sizeof(TrajectoryFrame) * p[element].count);
        thislen = __TrajectoryFrame_decode_array(buf, offset + pos, maxlen - pos, p[element].trajectory, p[element].count);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __Trajectory_decode_array_cleanup(Trajectory *p, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        __int16_t_decode_array_cleanup(&(p[element].speed), 1);

        __int16_t_decode_array_cleanup(&(p[element].distance), 1);

        __int16_t_decode_array_cleanup(&(p[element].mode), 1);

        __int16_t_decode_array_cleanup(&(p[element].count), 1);

        __TrajectoryFrame_decode_array_cleanup(p[element].trajectory, p[element].count);
        if (p[element].trajectory) free(p[element].trajectory);

    }
    return 0;
}

int Trajectory_decode(const void *buf, int offset, int maxlen, Trajectory *p)
{
    int pos = 0, thislen;
    int64_t hash = __Trajectory_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __Trajectory_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int Trajectory_decode_cleanup(Trajectory *p)
{
    return __Trajectory_decode_array_cleanup(p, 1);
}

int __Trajectory_clone_array(const Trajectory *p, Trajectory *q, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        __int16_t_clone_array(&(p[element].speed), &(q[element].speed), 1);

        __int16_t_clone_array(&(p[element].distance), &(q[element].distance), 1);

        __int16_t_clone_array(&(p[element].mode), &(q[element].mode), 1);

        __int16_t_clone_array(&(p[element].count), &(q[element].count), 1);

        q[element].trajectory = (TrajectoryFrame*) lcm_malloc(sizeof(TrajectoryFrame) * q[element].count);
        __TrajectoryFrame_clone_array(p[element].trajectory, q[element].trajectory, p[element].count);

    }
    return 0;
}

Trajectory *Trajectory_copy(const Trajectory *p)
{
    Trajectory *q = (Trajectory*) malloc(sizeof(Trajectory));
    __Trajectory_clone_array(p, q, 1);
    return q;
}

void Trajectory_destroy(Trajectory *p)
{
    __Trajectory_decode_array_cleanup(p, 1);
    free(p);
}

int Trajectory_publish(lcm_t *lc, const char *channel, const Trajectory *p)
{
      int max_data_size = Trajectory_encoded_size (p);
      uint8_t *buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = Trajectory_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = lcm_publish (lc, channel, buf, data_size);
      free (buf);
      return status;
}

struct _Trajectory_subscription_t {
    Trajectory_handler_t user_handler;
    void *userdata;
    lcm_subscription_t *lc_h;
};
static
void Trajectory_handler_stub (const lcm_recv_buf_t *rbuf,
                            const char *channel, void *userdata)
{
    int status;
    Trajectory p;
    memset(&p, 0, sizeof(Trajectory));
    status = Trajectory_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding Trajectory!!!\n", status);
        return;
    }

    Trajectory_subscription_t *h = (Trajectory_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    Trajectory_decode_cleanup (&p);
}

Trajectory_subscription_t* Trajectory_subscribe (lcm_t *lcm,
                    const char *channel,
                    Trajectory_handler_t f, void *userdata)
{
    Trajectory_subscription_t *n = (Trajectory_subscription_t*)
                       malloc(sizeof(Trajectory_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->lc_h = lcm_subscribe (lcm, channel,
                                 Trajectory_handler_stub, n);
    if (n->lc_h == NULL) {
        fprintf (stderr,"couldn't reg Trajectory LCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int Trajectory_subscription_set_queue_capacity (Trajectory_subscription_t* subs,
                              int num_messages)
{
    return lcm_subscription_set_queue_capacity (subs->lc_h, num_messages);
}

int Trajectory_unsubscribe(lcm_t *lcm, Trajectory_subscription_t* hid)
{
    int status = lcm_unsubscribe (lcm, hid->lc_h);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe Trajectory_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

